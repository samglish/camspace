/* Stub cordova.js pour les tests en navigateur bureau.
   Sur un vrai build Cordova, ce fichier est généré automatiquement
   par `cordova build` et REMPLACE celui-ci — ne pas le committer
   dans le repo final, il est régénéré à chaque build. */
console.log('[cordova-stub] Running outside a real Cordova shell (browser test mode).');
